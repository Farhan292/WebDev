<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <title>Film Reviews ⭐⭐⭐⭐⭐</title>
</head>
<body>
<nav class="navbar navbar-expand-lg">
    <a id="topTitle" class="navbar-brand" href="{{ path('home') }}">Film Reviews</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
        <ul class="navbar-nav">
            <li class="nav-item nav-link">
                <a class="nav-link" href="{{ path('home') }}">Home</a>
            </li>
            <li class="nav-item nav-link">
                <a class="nav-link" href="{{ path('register') }}">Register</a>
            </li>
            <li class="nav-item nav-link">
                <a class="nav-link" href="{{ path('films') }}">Films</a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown nav-link" id="loggedInIcon">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {% if app.user.username is defined %}
                            <img src="{{ asset('images/userpfp.jpg') }}" alt="user" id="userImg" class="rounded-circle" width="30" height="30">
                            {{ app.user.username }}
                        {% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                        <form action="{{ path('app_logout') }}" method="post" class="logout-form">
                            <button type="submit" class="btn btn-link logoutButton">Logout</button>
                        </form>
                        <a class="logoutButton btn btn-link" href="{{ path('myReviews') }}">My Reviews</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item nav-link">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
<style>
    #topTitle{
        color: aliceblue;
        font-size: x-large;
        padding-right: 5%;
    }
    @media (max-width: 992px) {
        .navbar-nav .nav-link {
            width: 100%;
        }
        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml;charset=UTF8,%3Csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='rgba%28255, 255, 255, 1%29' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3E%3C/svg%3E");
            padding: 0;
        }
        li{
            margin-top: 2px;
        }
    }
    .dropdown-menu {
        background-color: #404044;
    }
    li{
        width: 200px;
        text-align: center;
    }
    body {
        margin: 0;
        font-family: 'Arial', sans-serif;
    }
    .navbar-nav .nav-link {
        color: white;
        padding: 10px 16px;
        font-weight: bold;
        border-radius: 25px;
        line-height: 1.5;
    }

    .navbar-nav .nav-link:hover {
        background-color: #404044;
    }
    .logoutButton {
        background: none;
        color: white;
        cursor: pointer;
        margin-left: 10px;
        width: 90%;
        border: 1px;
    }

    .logoutButton:hover {
        background-color: grey;
        text-decoration: none;
        color: black;
    }

    #userImg {
        width: 25px;
        height: 25px;
        border-radius: 50%;
        margin-right: 5px;
    }

    .logout-form {
        display: block;
    }

</style>
{% block content %}
    content
{% endblock %}

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
